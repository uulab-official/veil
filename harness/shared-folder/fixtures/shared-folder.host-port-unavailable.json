{
  "kind": "vmSharedFolderStatus",
  "generatedAt": "2026-08-04T09:26:14Z",
  "provider": "QEMU/HVF",
  "vmState": "stopped",
  "readiness": "unavailable",
  "capability": {
    "state": "hostPortUnavailable",
    "isSupported": false,
    "transport": "guest-smb",
    "isWiredIntoBootPlan": false,
    "shareName": "VeilShared",
    "expectedGuestDirectoryPath": "C:\\VeilShared",
    "hostMountURL": "smb://127.0.0.1:18445/VeilShared",
    "expectedHostMountPath": "/Volumes/VeilShared",
    "hostForwardClause": "hostfwd=tcp:127.0.0.1:18445-:445",
    "overrideEnvironmentVariable": "VEIL_QEMU_SHARED_FOLDER",
    "detail": "Host port 18445 is already in use, so the forward was left out of the boot plan instead of stopping QEMU from starting."
  },
  "hostMount": {
    "isMounted": false,
    "detail": "/Volumes/VeilShared is not present, so the share is not mounted on this Mac."
  },
  "hostStagingFolderPath": "/Users/example/Veil Shared",
  "nextActions": [
    "Host port 18445 is in use, so the forward was left out of the boot plan.",
    "Find the listener with: lsof -nP -iTCP:18445 -sTCP:LISTEN",
    "Free the port, then restart the VM so the plan is rebuilt with the forward."
  ]
}
