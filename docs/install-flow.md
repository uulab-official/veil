# Windows Arm Install Flow

Veil's install flow is designed around a bring-your-own Windows 11 Arm model. The product goal is not to hide licensing or media ownership. The goal is to make the setup path explicit, recoverable, and ready for a local runtime provider implementation.

## Product Intent

Users should not manage a generic VM app once setup is complete. They should prepare Windows once, install the Veil guest agent once, and then use Windows apps through the macOS host shell.

## v0.1 Setup Model

The macOS host stores a local VM profile and reports five setup steps:

1. Windows 11 Arm installer
2. Virtual disk
3. macOS shared folder
4. Automatic install media
5. Veil guest agent

The first four are local host prerequisites. The guest agent step remains pending until Windows can boot and the agent installer exists.

## Current Host Behavior

- Prepare VM creates the default Windows 11 Arm profile, the macOS shared folder at `~/Veil Shared`, and the default sparse disk in one action.
- Prepare VM does not scan Downloads or any other broad user folder for installer media. The user chooses the Windows ISO explicitly in the host app, or passes it through `veil-vmctl prepare --installer <path>`.
- Prepare VM creates `~/Veil Shared/Autounattend.xml` with Windows Setup language/OOBE inputs, Windows 11 Pro image selection, UEFI/GPT disk partitioning for the blank VM disk, an offline OOBE registry fallback, and no product key value.
- Prepare VM creates `~/Veil Shared/VeilAutoInstall.iso`, a small local ISO containing `Autounattend.xml` and, when available, the Veil guest-agent bootstrap payload. Veil only rebuilds this media when setup or guest-agent recovery payloads are needed and have changed.
- Prepare VM applies an adaptive resource profile from the current Mac: half of host CPU cores up to a safe cap, 25% of physical memory rounded down to a conservative VM cap, and a 128 GB default sparse disk.
- `veil-vmctl prepare --installer <path>` prepares the same local profile, shared folder, default sparse disk, installer path, and diagnostics bundle from the command line.
- `veil-vmctl prepare --installer <path> --drivers <path>` also stores an optional user-provided Windows driver ISO path. Veil attaches that media read-only during QEMU/HVF boots, but does not download, bundle, or license driver media.
- Profile-only creation is still available for low-level setup testing.
- Installer media is a user-selected local file. Veil stores the selected path and a security-scoped bookmark in the local profile, but does not copy, redistribute, or auto-discover Windows media.
- VM start resolves stored security-scoped bookmarks before file validation and local QEMU/HVF launch, reducing repeated macOS file-access prompts after the user has explicitly selected the ISO, driver media, or disk.
- If an older profile points at `~/Downloads` media without a stored security-scoped bookmark, Veil blocks boot readiness and asks the user to re-select the ISO in the app, or re-register it with the concrete `veil-vmctl prepare --installer ...` command surfaced in runtime diagnostics, instead of touching the protected Downloads path during snapshot loading.
- Snapshot loading also ignores stale QEMU console screenshots under `~/Downloads` so the app can open without macOS prompting for Downloads access; diagnostics should live under Veil's Application Support directory unless the user explicitly exports them.
- The virtual disk can be user-selected or created as a blank sparse disk at `~/Virtual Machines/Veil/Windows 11 Arm.img`.
- The boot spike stores EFI variables and the generic machine identifier next to the virtual disk so repeated boots keep stable VM identity.
- The host now prefers the local QEMU/HVF compatibility provider when it is installed and ready, because that is the clearest path to a UTM-style visible Windows installer console. Apple Virtualization remains the fallback feasibility provider.
- While a QEMU/HVF VM is starting or running, the host shell quietly refreshes the latest VM-console screenshot through the recorded monitor socket so the main window can keep showing Windows setup progress without requiring the separate QEMU display to be frontmost.
- The runtime snapshot reports structured setup steps so the UI can show what is complete, blocked, or pending.
- The runtime snapshot reports preflight checks for installer media, guest OS, CPU, memory, and disk size. Installer media is required before Windows is installed; after the profile is marked installed, the ISO is no longer required for normal boot.
- Before installation, a profile becomes boot-ready only when installer media, virtual disk, shared folder, automatic install media, and preflight checks all pass. After installation, boot readiness is based on the installed system disk, shared folder, and non-installer preflight checks.
- Pressing Start in the macOS app builds the active local runtime plan and starts QEMU/HVF in headless single-window preview mode. The app keeps runtime status and screenshot evidence in the main Veil window; set `VEIL_USE_NATIVE_QEMU_DISPLAY=1` to opt into the temporary Cocoa display fallback.
- Before installation, QEMU/HVF attaches the user-provided Windows ISO, the generated automatic install ISO, optional external driver media, and the writable NVMe system disk when starting the VM. After Windows is installed, Veil boots from the NVMe system disk and does not attach the Windows installer ISO. If the guest agent is already connected, Veil also stops attaching automatic install media. NVMe is used for the install-time system disk because the Windows 11 Arm installer sees it without a separate VirtIO storage driver. The default QEMU network adapter remains `usb-net`, but when external driver media is configured and `VEIL_QEMU_NETWORK_DEVICE` is not explicitly set, the local QEMU plan now selects `virtio-net-pci` so the attached virtio-win NetKVM driver can bind to the NIC. `VEIL_QEMU_NETWORK_DEVICE` can still select `e1000`, `e1000e`, `rtl8139`, `vmxnet3`, `virtio-net-pci`, or `virtio-net-device` for bounded live compatibility probes. The boot plan reports both `networkAdapter` and `networkDeviceArgument` so diagnostics show exactly which local NIC model was used.
- Apple Virtualization can still build a `VZVirtualMachine` with the same profile, ISO, automatic install media, and writable disk, but it is no longer the preferred visible-console path while Windows installer display support remains unproven.
- While the VM is running with native display fallback enabled, Open Recovery Display in the app menu or menu bar brings the active QEMU Cocoa window forward. In the default app path there is no second native display window, and the main VM screen does not expose a native-display control.
- While the VM is running and the guest agent is not connected, the host shell exposes an Install Guest Agent recovery action in the VM controls and menu bar. It sends the same bounded QMP/HMP key sequence as `veil-vmctl qemu-install-agent`, so a visible Windows desktop can run the `VEIL_AUTO` bootstrap without requiring the user to leave the app.
- While the VM is running, the host periodically retries the live guest-agent health check. When the Windows agent becomes reachable, Veil leaves demo mode and records guest-agent install evidence on the VM profile.
- The Windows app-window launcher accepts a Notepad open request before the live guest agent is connected, but stores it as a pending intent instead of showing demo mirroring. When the live agent connects, Veil sends the real launch request and opens the macOS mirror window from the accepted HWND.
- When a Windows app launch is queued and the local VM is already running without a live guest agent, the app shell now sends the same bounded attached-media `V.cmd` recovery path used by `veil-vmctl qemu-install-agent` once for that QEMU runtime. The reconnect poller then fulfills the pending app launch after `agent.health.response` is available, so the normal app path moves toward a Parallels-style "open app, recover runtime in the background" flow instead of leaving the user at a waiting state.
- The queued-app waiting state stays user-actionable: the main launcher play button changes to a guest-agent repair action and the menu bar shows Continue Queued App. Both use the same attached-media recovery path before the reconnect poller opens the mirrored macOS app window.
- The post-install launcher treats Open Windows App as the start of the app-open flow even before the live guest agent is connected. The UI can queue the selected app, show Agent as repair/waiting, and avoid claiming the agent is connected until a real `agent.health.response` arrives.
- Queued app launches that still need Windows start, guest-agent wait, or guest-agent repair no longer pass the release review gate. `openWindowsApp` stays actionable and points at the concrete start/repair/wait command, but review readiness only returns after the selected app can launch through a live connection or a mirrored macOS app window exists.
- If the local VM still reports `running` but the embedded console preview is stale or unavailable, app-runtime status reports `localRuntime.recommendedAction=recover-runtime-display` with `qemu-display-smoke` and `qemu-capture` recovery commands. This prevents the app-open path from treating a black/stale VM display as a healthy Parallels-style runtime.
- The same stale/unavailable preview also blocks the one-screen release gate: `windowsSetup` must point at Refresh Display until display evidence is current, so the app cannot report review readiness while the Windows surface may still be black or stale.
- A successful `qemu-capture` now stores `consoleScreenshotRefreshedAt` in the latest QEMU launch record. Follow-up app-runtime status polling preserves that refresh evidence, letting the launcher leave Refresh Display recovery once the console screenshot has been refreshed.
- If the VM is running with an old or missing `VeilAutoInstall.iso`, app-runtime status now reports `localRuntime.requiresGuestToolsMediaRebuild=true`, blocks `runtime.repairGuestAgentForApp`, and moves the one-screen primary action to `app-runtime-action --action stop-runtime`. After Windows stops, the same status carries the `veil-vmctl prepare --installer ...` rebuild command so the next boot attaches current guest tools before app connection repair.
- Rejected launch and wait-agent action reports follow the same rule: stale guest tools media next actions tell the operator to stop Windows, rebuild the attached media, refresh app-runtime status, and only then retry app connection repair.
- `veil-vmctl app-runtime-action --json --action recover-display` runs the same safe console refresh through the app-runtime action surface and returns `displayRecovery` evidence. The action is accepted only when the refreshed runtime evidence becomes `fresh`, so automation and future app controls cannot mark a black/stale display as recovered by accident.
- The app shell now exposes the same stale-display recovery as Refresh Display in the main play flow, the toolbar, the app command menu, and the menu bar. When display evidence is stale, app-open commands are gated behind that refresh instead of launching into a black or stale VM surface.
- `veil-vmctl app-runtime-action --json --action reconnect-restore` makes the Coherence restore loop explicit: if previous Windows app ids are persisted, automation can reconnect to the guest agent and restore their HWND-backed macOS windows through the same app-runtime action surface. A live retry on July 5, 2026 found the persisted Notepad intent but still returned `accepted=false` because the guest agent was unreachable, leaving `guest-agent-wait` and `veil-host-probe --diagnose-agent` as the next actions.
- `veil-vmctl app-runtime-action --json --action wait-agent --wait-seconds <n>` wraps the same post-install guest-agent readiness gate inside the app-runtime action surface. It returns `agentWait` with the forwarded endpoint, attempt count, host-forward probe, and recovery actions, and is accepted only when `agent.health.response` is reachable.
- `./script/build_and_run.sh --start-vm` launches the signed app bundle with the prepared profile and starts the VM automatically.
- Pressing Stop stops the active local VM process and closes host-side app bridge windows.
- Each Start attempt records a metadata-only boot report with timestamps, result, resulting runtime state, selected profile, planned devices, and error text when startup fails.
- Export Diagnostics writes a JSON bundle with host metadata, the runtime snapshot, setup steps, preflight checks, the stored VM profile, and the latest boot report to a user-selected diagnostics directory.
- Start requires a locally signed app bundle with the `com.apple.security.virtualization` entitlement.

The adaptive resource profile is an initial configuration policy, not live VM hot-resizing. The local runtime provider can use host memory on demand under the configured VM memory cap, and future work can add app-specific profiles, suspend/resume policy, and telemetry-driven adjustments once the real Windows path is stable.

Diagnostics bundles and boot reports are metadata only. They may include local file paths, VM device roles, runtime state, and startup error text so maintainers can understand setup state, but they must not copy Windows installer media, virtual disk bytes, product keys, or guest user data.

## Preflight Checks

Before the VM boot implementation lands, Veil already blocks obviously invalid profiles:

- Before installation, installer media must be a local bootable ISO file. VHD/VHDX files are treated as disk images, not installer media. After Windows is installed, this check becomes informational and the ISO may be removed from the normal profile.
- Guest OS must be `windows-arm64`.
- CPU allocation must be at least 2 virtual CPUs.
- Memory allocation must be at least 4096 MB.
- Disk size must be at least 64 GB.

These checks are deliberately conservative. They catch configuration mistakes before the local runtime provider tries to build or start a VM.

## Windows Display Reality Check

The v0.1 boot spike can create and start a `VZVirtualMachine`, attach ISO and disk storage, and open Apple's `VZVirtualMachineView`. That is not the same as a UTM-grade Windows installer path. Apple's public Virtualization documentation focuses on Linux and macOS guest flows, and Windows 11 Arm installer display/driver behavior through the Apple Virtio graphics path must be proven with real media before Veil can claim reliable Windows setup.

The QEMU/HVF compatibility spike has progressed past static planning: on July 1, 2026, a local Homebrew QEMU 11.0.2 install was validated with `qemu-system-aarch64`, Arm EDK2 firmware at `/opt/homebrew/share/qemu/edk2-aarch64-code.fd`, the user-provided `Win11_25H2_Korean_Arm64_v2.iso`, and a separate sparse QEMU test disk. The ISO contains `efi/boot/bootaa64.efi` and macOS reports it as an AArch64 EFI application.

Current QEMU boot evidence:

- QEMU can start the local device graph with HVF, Arm UEFI pflash code, a VM-local writable `uefi-vars.fd`, lock-safe read-only Windows ISO media, generated automatic install ISO media, writable raw NVMe system disk, NAT networking, Cocoa or headless ramfb graphics, USB input, a local `swtpm` TPM 2.0 emulator, and serial logging.
- `veil-vmctl qemu-start --wait-seconds <n>` launches the stored Windows Arm profile with QEMU's display hidden behind a loopback VNC endpoint for Veil's embedded setup surface. It keeps an HMP monitor socket open for screenshots, attaches a QMP socket for structured key input on new launches, sends boot-prompt key input, and records both the VM-console screenshot path and loopback display endpoint. Use `--native-display` only for advanced recovery when the raw QEMU Cocoa window is needed.
- `veil-vmctl qemu-capture` refreshes the latest running QEMU launch screenshot through the recorded HMP monitor socket without sending a quit command, so the app and diagnostics can follow long Windows setup/OOBE runs.
- `veil-vmctl qemu-install-status --json` reads the current runtime snapshot and latest QEMU launch record into a metadata-only Windows install status report. It includes boot readiness, install evidence, automatic install media freshness, latest console screenshot path, latest display endpoint when available, planned 1440x900 guest framebuffer evidence, aspect-fit host-window scaling, Retina host-rendering policy, running QEMU process evidence when the configured disk is already attached, and safe next actions such as `qemu-display-smoke`, `qemu-capture`, `qemu-oobe-bypass`, or `qemu-install-agent`. It does not start, stop, copy, or modify Windows media or virtual disks.
- If `VeilAutoInstall.iso` is older than the staged `Autounattend.xml` or `Veil Guest Agent` bundle, install status reports `automaticInstallMediaStatus.state=stale` and moves the next action toward `qemu-powerdown`, `veil-vmctl prepare --installer ...`, and relaunch. This turns the common "repair still uses the old attached media" trap into a visible product diagnostic instead of a hidden operator memory.
- When install status detects a running QEMU process for the configured disk but no current launch record, it reports the process PID and any detected HMP/QMP socket paths before installer or preflight blockers. This protects the disk from relaunch attempts and makes the operator sequence explicit: close the existing Windows/QEMU PID, then reselect protected media or rerun readiness checks.
- The main Windows setup screen renders the same install-status next actions as compact recovery steps, so blocked setup states are visible in the app without requiring a terminal command.
- `veil-vmctl qemu-powerdown --wait-seconds <n>` requests ACPI shutdown through the recorded QMP socket on new launches, falls back to HMP for older launches, and waits for the recorded QEMU PID to exit. `qemu-start` now refuses to launch a second process while the latest recorded QEMU PID is still alive, protecting the Windows disk from accidental double attachment.
- If an older QEMU process is still alive but its launch record is missing or was written to an old diagnostics directory, Veil also scans running `qemu-system-aarch64` commands for the configured Windows disk path and blocks a new launch when the same disk is already attached.
- `veil-vmctl qemu-force-stop --i-understand-data-loss --wait-seconds <n>` is the last-resort local recovery command for a QEMU process that cannot be shut down through monitor control. It sends `SIGTERM`, records whether the PID exits within the wait window, and refuses to run without the exact risk acknowledgement flag.
- The main Veil app Start action now launches the same local QEMU/HVF console path with the embedded display as the default surface.
- The main Veil app Stop action now also checks the latest QEMU launch record and sends `SIGTERM` to a matching live QEMU PID for the current virtual disk, so CLI-started VMs can be stopped from the app instead of leaving the Stop control detached from the actual local runtime.
- When the same ISO is already attached to another VM, QEMU needs the file-driver form `file.locking=off` for read-only ISO reuse.
- Earlier boot attempts reached Arm UEFI and mapped the installer ISO as `FS0`, but Windows Setup did not start because UEFI reported a boot image timeout and fell back to the EDK II shell.
- USB storage, SCSI CD-ROM, and virtio-blk installer attachment variants were tested against the local `Win11_25H2_Korean_Arm64_v2.iso`; without boot-key input, each reached UEFI and then fell back to the EDK II shell.
- The boot timeout was traced to the Windows installer boot prompt requiring a key press. Veil now adds a short QEMU monitor socket for the app launch path and bounded smoke path, then sends boot key input immediately after start.
- Veil sends that installer boot key only for a fresh or still-sparse disk. If the profile is marked installed or the virtual disk has already allocated at least 1 GiB, Veil treats the disk as partially installed or installed and lets the firmware boot through to the NVMe disk instead of forcing the ISO prompt again.
- With that app launch path, the local QEMU/HVF console reaches the Korean Windows 11 Setup product-key screen on July 1, 2026.
- `virt,highmem=off` with more than 3 GB memory fails under HVF because address space is limited. A 3 GB `highmem=off` attempt reaches UEFI but still does not start Windows Setup.
- `veil-vmctl qemu-smoke --json --seconds 25` now repeats the headless QEMU attempt in snapshot mode, sends bounded boot-prompt key input, writes serial/process logs plus a console PNG, and classifies the current result with `boot-prompt-key-sent` evidence.
- On July 2, 2026, the bounded QEMU smoke run with the local `Win11_25H2_Korean_Arm64_v2.iso` produced a console PNG showing the Korean Windows 11 Setup product-key screen. The serial classifier remained `runningNoDecision`, so the screenshot is the authoritative evidence for that run.
- After adding `ProductKey/WillShowUI=Never` without a `ProductKey/Key` value, a 60 second bounded QEMU smoke run advanced past the product-key prompt and reached the Korean Windows 11 requirements failure page.
- After installing `swtpm` and adding QEMU `tpm-tis-device` plus `-tpmdev emulator`, a 120 second bounded QEMU smoke run recorded `tpm2-detected` evidence and the Windows Setup requirements page dropped the TPM 2.0 failure. The remaining visible blocker is Secure Boot support.
- After switching from `-bios` to pflash code plus VM-local writable vars, another 120 second bounded QEMU smoke run kept `boot-prompt-key-sent`, `tpm2-detected`, and `qemu-running` evidence. The visible setup blocker stayed Secure Boot. Homebrew QEMU 11.0.2 ships an AArch64 EDK2 descriptor with pflash vars, but that descriptor does not advertise `secure-boot`; Veil now reports that as a doctor warning instead of hiding it in generic boot failure text.
- Veil now follows UTM's AArch64 Secure Boot firmware pattern more closely: Secure Boot is treated as a pair of `edk2-aarch64-secure-code.fd` plus `edk2-arm-secure-vars.fd`. The host discovers secure code from a user-local Veil firmware cache, UTM.app, or local QEMU resource paths, and discovers secure vars from the Veil firmware cache or UTM.app.
- Veil still prefers the secure vars template for new Windows 11 Arm profiles, replaces an existing pre-install `uefi-vars.fd` when upgrading to secure vars, and pads small templates to QEMU's 64 MiB pflash backend size. However, it does not mark Secure Boot firmware available unless matching secure code is also present.
- Adding the secure vars candidate and UTM-style `virtio-rng-pci` device is not yet enough to claim Secure Boot support. A July 2, 2026 120 second live `qemu-smoke` with the secure vars template still recorded `boot-prompt-key-sent`, `tpm2-detected`, and `qemu-running`, while the console PNG still showed the Korean Windows Setup Secure Boot requirement. The serial log also still reported `ArmTrngLib could not be correctly initialized.`
- On the current test Mac, Homebrew QEMU 11.0.2 has `/opt/homebrew/share/qemu/edk2-aarch64-code.fd` but no `edk2-aarch64-secure-code.fd`; `qemu-doctor` now reports that exact missing half of the pair.
- A local-only Ubuntu AAVMF package (`qemu-efi-aarch64_2024.02-2ubuntu0.7_all.deb` from `https://security.ubuntu.com/ubuntu/pool/main/e/edk2/qemu-efi-aarch64_2024.02-2ubuntu0.7_all.deb`) provided `AAVMF_CODE.secboot.fd` and `AAVMF_VARS.ms.fd`. These files were copied into the user-local Veil firmware cache as `edk2-aarch64-secure-code.fd` and `edk2-arm-secure-vars.fd`; the firmware binaries are not committed.
- With that secure code plus secure vars pair, a July 2, 2026 120 second bounded `qemu-smoke` reached the Korean Windows 11 Setup disk-selection screen. The earlier Secure Boot and TPM requirements page was gone, so the next visible blocker became storage discovery rather than Windows 11 requirements.
- The disk-selection screen showed no available install target while the system disk was attached as `virtio-blk-pci`. Veil now follows the UTM-style QEMU NVMe device pattern for the install-time system disk (`-device nvme,drive=system,serial=veil-system`) so Windows Setup can use its inbox NVMe storage driver instead of requiring a VirtIO block driver during setup.
- After switching to NVMe, a July 2, 2026 120 second bounded `qemu-smoke` produced a console PNG showing `Disk 0 Unallocated Space` as a 128.0 GB Windows Setup install target. The serial classifier remained `runningNoDecision`, so the console PNG is the authoritative evidence for the storage checkpoint.
- Veil's generated `Autounattend.xml` now includes UEFI/GPT partition creation for Disk 0, `InstallTo` Disk 0 Partition 3, `WillShowUI=Never` for DiskConfiguration, ImageInstall, and ProductKey, plus a specialize-pass `BypassNRO` registry fallback while still omitting any product-key value. A follow-up July 2, 2026 bounded live `qemu-smoke` advanced to the Korean `Windows 11 installing` screen and showed 32% complete. A persistent visible `qemu-start` run then continued on the real VM disk, grew the sparse disk past 12 GB, reached the first post-reboot Windows setup phase, showed `Installing 94%`, and advanced to the Korean OOBE network screen. That OOBE evidence proved the install reached the first user setup phase, and exposed a network-driver blocker: the tested Windows 11 Arm image did not expose usable network connectivity for `virtio-net-pci`, `e1000`, or `usb-net` during OOBE.
- Veil now lets the host profile reference an optional external driver ISO and QEMU/HVF attaches it as read-only USB mass storage with drive id `drivers`. Fedora's virtio-win project publishes Windows VirtIO driver ISO downloads, including stable ISO links, but Veil treats those as external user-provided files and does not commit or redistribute them.
- The first OOBE recovery attempts through the HMP monitor `sendkey` path recorded sender evidence but did not dismiss the live folder-selection dialog. New QEMU launches now also attach a QMP socket and `veil-vmctl qemu-sendkey` prefers QMP `input-send-event` key down/up events when that socket exists, falling back to HMP `sendkey` only for older launch records.
- The current pre-QMP live launch also rejected the bounded HMP `system_powerdown` shutdown attempt: `qemu-powerdown --wait-seconds 10` recorded `transport=hmp`, `terminationStatus=1`, and `didExitWithinWait=false`. `qemu-force-stop` now exists for this last-resort transition, but it requires the explicit `--i-understand-data-loss` acknowledgement before signaling the recorded QEMU PID. This reinforces that the next live recovery pass needs a QMP-attached relaunch before judging the new structured input path.
- A July 2, 2026 QMP-attached relaunch force-stopped the old pre-QMP process, started PID 20933 with QMP, returned to the Korean OOBE network screen, and opened an Administrator command prompt with `shift-f10`. After the manual `oobe\bypassnro` recovery command triggered a reboot, the previous ISO-first launch policy left firmware at TianoCore instead of returning to the installed Windows disk. Veil now switches launch arguments from `-boot order=d` to `-boot order=c` when the disk is already partially installed or installed; a follow-up launch recorded `-boot order=c` and returned to the Windows OOBE network screen from the local NVMe disk.
- `qemu-oobe-bypass` now sends a staged key sequence that first dismisses modal dialogs with `esc`, waits longer after `shift-f10`, and then sends `oobe\bypassnro`. The QMP text input blocker was traced to `send-key`: switching the QMP path to documented `input-send-event` key down/up payloads typed into the elevated Windows command prompt, executed `oobe\bypassnro`, and rebooted the guest.
- A follow-up July 2, 2026 live run used QMP `input-send-event` to click the OOBE "I do not have internet" path, create a local `veil` user with a blank development password, and reach the Windows 11 Arm desktop. The proof screenshot is `/tmp/veil-windows-desktop-candidate.png` on the development Mac. This is local feasibility evidence, not a committed artifact.
- Veil's automatic install ISO now includes the staged `Veil Guest Agent` bundle when it exists next to `Autounattend.xml`, so a future Windows desktop boot can access the installer from the attached `VEIL_AUTO` media instead of relying on an unavailable live macOS folder share. The Windows agent installer now prefers a packaged `app/VeilAgent.exe` bundle and only falls back to `dotnet publish` when no packaged app exists. `scripts/publish-veil-agent-bundle.sh` and `scripts/Publish-VeilAgentBundle.ps1` create that win-arm64 bundle on a machine with the .NET 8 SDK. `scripts/Build-VeilAgentSparsePackage.ps1` creates a signed sparse identity package when the Windows SDK and a signing certificate are available; the staged `Prepare Sparse Package.cmd` launcher writes those signing artifacts to `%LOCALAPPDATA%\Veil\Agent\package` rather than the read-only ISO, then reruns `Install-VeilAgent.ps1` with explicit sparse-package paths so the installer can register the package with `Add-AppxPackage -ExternalLocation` against the installed app folder. A successful registration is what lets `agent.health.response.capabilities.packageIdentity` become a real live readiness signal. Generated `Autounattend.xml` also adds a first-logon command that discovers the `VEIL_AUTO` volume by label and runs `Veil Guest Agent\scripts\Bootstrap-VeilAgentFromMedia.ps1`, so future clean installs can install and start the agent without manual File Explorer steps. The bootstrap, install, and start scripts write logs under `%LOCALAPPDATA%\Veil\Agent\logs` so failed first-run installs leave guest-side evidence. A local July 2, 2026 build produced the ignored `apps/windows-agent/app/VeilAgent.exe` payload and a regenerated `VeilAutoInstall.iso`; mounting the ISO verified `Autounattend.xml`, the install command, and the packaged agent executable. A live current-desktop QMP input attempt did not yet produce guest-agent health, so the accepted next checkpoint remains a successful `veil-host-probe` response from Windows.
- The guest-agent start path is now safe to run repeatedly: the script reuses an already-running installed `VeilAgent.exe`, the agent process enforces one instance per configured forwarded port, the script waits for the localhost WebSocket port to become reachable, and logs include `agent.stdout.log` plus `agent.stderr.log` next to the install/start logs.
- Guest-agent installation treats logon task registration as best effort: if Windows denies `Register-ScheduledTask` for the current non-elevated user, Veil logs the warning and still starts the current-session agent. The installer uses the valid scheduled-task run level `Limited` instead of the invalid `LeastPrivilege` value.
- The installer stops any already-running `VeilAgent.exe` before replacing the installed app bundle, so repeated install attempts from the visible Windows desktop do not fail on locked runtime DLLs.
- When the installer runs elevated, it also adds a Windows Firewall inbound rule for the installed `VeilAgent.exe`. Standard-user installs keep going and log a warning, but the host-forwarded WebSocket may remain TCP-open/no-response until the user runs `Repair Veil Agent Connectivity.cmd` and approves the Windows administrator prompt. The repair path refreshes both the program rule and a TCP 18444 port rule, then restarts the installed agent.
- The Windows agent now avoids `HttpListener` URL ACL behavior and binds a raw WebSocket listener to `0.0.0.0:18444` inside the guest. The start script still probes `127.0.0.1:18444` inside Windows, while the macOS host connects to `ws://127.0.0.1:18444` through QEMU `hostfwd`.
- Host-side guest-agent diagnostics now separate TCP reachability from protocol health. A `diagnostic.hostForwardProbe.status` of `tcpOpen` with a health timeout means macOS can reach QEMU's forwarded port, but Windows Firewall, guest NIC state, or the guest WebSocket listener is still preventing `agent.health.response` from completing.
- QEMU network adapter selection is now explicit in the dry-run plan and harness. The default still uses `usb-net`, while live debugging can relaunch with `VEIL_QEMU_NETWORK_DEVICE=e1000e` or another supported candidate without changing source code. This is a diagnostic lever, not a release claim that every Windows 11 Arm image ships an inbox driver for every candidate.
- A July 4, 2026 live probe relaunched the installed Windows disk with `VEIL_QEMU_NETWORK_DEVICE=e1000e`. QEMU started with `-device e1000e,netdev=net0`, the VNC display smoke passed for PID 14085, and `guest-agent-wait` again reported `diagnostic.hostForwardProbe.status: "tcpOpen"` plus a WebSocket health timeout. That narrows the current blocker away from the host-side QEMU port forward shape and toward Windows Firewall, the guest-side agent process, or elevated guest installation.
- A follow-up July 4, 2026 repair pass regenerated `VeilAutoInstall.iso` with `Repair Veil Agent Connectivity.cmd`, `Repair-VeilAgentConnectivity.ps1`, and the short `V.cmd` automation entrypoint. After relaunching QEMU as PID 29578, `qemu-install-agent` sent the `V.cmd` path in 145 QMP key events instead of the earlier 384-key repair/install command. The guest-agent wait result still stayed `tcpOpen` plus WebSocket timeout, and the post-attempt console capture showed the Windows desktop rather than a visible UAC or PowerShell window. The next live automation step is screenshot-backed Run/UAC detection or a deterministic guest command channel.
- A later July 4, 2026 pass fixed the repair launcher so the non-elevated PowerShell process no longer holds `repair.log` open while requesting UAC elevation, and added a bounded automatic UAC approval tap to `qemu-install-agent`. A live retry on QEMU PID 5279 recorded `activationTap=true`, `uacApprovalTap=true`, `keyCount=145`, and post-attempt console evidence returning to the Windows desktop. The host-forwarded endpoint still reported `tcpOpen` plus WebSocket health timeout, so the next blocker is no longer UAC launch itself; it is proving elevated repair completion, agent process state, or the guest-side WebSocket handshake.
- The next repair hardening pass made that proof explicit. `Repair-VeilAgentConnectivity.ps1` now passes a `StatusPath` into the elevated process, waits from the original non-elevated console for `%LOCALAPPDATA%\Veil\Agent\logs\repair-status.json`, and records `started`, `firewallRulesReady`, `guestAgentHealthSucceeded`, or `failed`. `Start-VeilAgent.ps1` no longer treats an open TCP port as success by itself; it sends `agent.health.request` to `ws://127.0.0.1:18444/` and to a non-loopback Windows guest IPv4 address, requiring both `agent.health.response` probes before the repair status can become successful.
- Live evidence after the first in-guest WebSocket probe showed `guestAgentHealthSucceeded` on Windows loopback while macOS still timed out through QEMU `hostfwd`. That moves the current blocker to guest NIC/IP reachability or the host-forward boundary, not the Windows agent process itself.
- The first non-loopback IPv4 probe did not appear in the live console because the repair flow preferred the already installed `%LOCALAPPDATA%\Veil\Agent\scripts\Start-VeilAgent.ps1` over the refreshed script on the new ISO. The repair flow now refreshes installed support scripts from the current media before restarting the agent, so repeated `qemu-install-agent` attempts pick up diagnostic changes without requiring a full reinstall.
- The repair flow now also refreshes the installed `%LOCALAPPDATA%\Veil\Agent\app` bundle when the current media contains `app/VeilAgent.exe`, so repeated `qemu-install-agent` attempts can pick up agent binary changes as well as script changes.
- After regenerating the ISO and relaunching with that support-script refresh, the live console reached the refreshed check and failed with `Guest IPv4 addresses: none`. That confirms the current host connection blocker is Windows Arm guest NIC/IP availability on the tested QEMU `e1000e` path; macOS can see QEMU's forwarded TCP socket, but the guest has no usable non-loopback IPv4 for the forwarded WebSocket to reach. The next coded recovery path is driver-media first: if a user-provided virtio-win ISO is attached, QEMU/HVF now uses `virtio-net-pci`, and `Repair-VeilAgentConnectivity.ps1` searches attached drives for `NetKVM\w11\ARM64` and runs `pnputil /add-driver ... /install` before restarting the agent and rechecking guest IPv4 health. The driver install step logs non-zero `pnputil` exits and continues with firewall plus agent health checks, because the driver may already be installed or pending Windows device state.
- A live retry with the new media on QEMU PID 39131 proved the previous UAC click path was still too weak: the post-attempt screenshot showed the Korean Windows administrator prompt with No focused, even though the pointer had moved to Yes. Sending `veil-vmctl qemu-sendkey --json left ret` dismissed the prompt, so `qemu-install-agent` now records and sends that keyboard approval after the pointer tap. `V.cmd` also keeps its console visible briefly after the repair command returns, giving `postAttemptConsole` a chance to capture `guestAgentHealthSucceeded`, `failed`, or timeout text instead of an ambiguous desktop-only screenshot.
- A July 4, 2026 live driver-media retry attached `/Users/bonjin/Downloads/virtio-win.iso`, relaunched QEMU with `virtio-net-pci`, ran `qemu-install-agent`, and received `agent.health.response` from the Windows 11 Arm guest through `ws://127.0.0.1:18444`. This proves the current blocker is no longer VM boot, UAC approval, basic firewall repair, or host-forwarded guest-agent health.
- The same live path exposed a localized app-window detection bug: `app-window-proof --wait-seconds 8` returned a bounded `launchTimeout` while the Windows desktop visibly opened Korean Notepad. The Windows agent now snapshots existing matching app windows before launch, then prefers newly created top-level windows that match either the launched process, the executable process name, or the title. This avoids depending on an English title such as `Notepad`.
- A follow-up July 4, 2026 run republished the win-arm64 agent bundle, regenerated `VeilAutoInstall.iso`, relaunched QEMU with `virtio-net-pci` plus the user-provided virtio-win driver ISO, reinstalled/repaired the agent, and proved the first real app-window loop. `veil-vmctl app-window-proof --json --app-id winapp_notepad --wait-seconds 15` returned `app.launch.response`, `window.created` for `hwnd:000200B0`, and a 600 x 393 PNG frame from the Windows 11 Arm guest. `veil-vmctl mvp-proof --json --app-id winapp_notepad --wait-seconds 15 --require-proved` then returned `status=proved` with guest-agent health, Notepad HWND tracking, initial frame, post-input frame, mouse input, keyboard input, and host-to-guest clipboard text.
- A later July 4, 2026 app-runtime retry exposed another real runtime edge: continuing `window.frame` stream messages can arrive before a fresh app-launch reply on the same WebSocket. The host transport now ignores unsolicited stream/update/close/clipboard events while waiting for direct request replies, and the launch parser selects messages by protocol type instead of positional decoding. A live `veil-vmctl app-runtime-action --json --action launch --app-id winapp_notepad` then returned `accepted=true`, a Korean Notepad HWND, live app-window status, and the launcher-hidden policy; `veil-host-probe --launch-notepad-frame` also returned launch metadata plus the first PNG frame.
- `veil-vmctl qemu-install-agent --json [--wait-seconds 30]` now provides a single bounded QMP recovery command for a visible Windows desktop: it focuses the desktop through QMP pointer input, opens Run with the Windows key, scans attached drive letters for the short `Veil Guest Agent\V.cmd` automation entrypoint, and invokes it. `V.cmd` runs `Repair Veil Agent Connectivity.cmd` when present and falls back to `Install Veil Agent.cmd` for older media layouts. The command records the desktop activation tap, bounded key-send evidence, a bounded UAC approval tap, guest-agent wait result, and `postAttemptConsole` screenshot evidence so failed attempts show whether Windows displayed Run, UAC, PowerShell, or only the desktop.
- A July 10, 2026 KST live retry found the same operational trap again: the already-running Windows guest still failed guest-agent health after repair, but republishing `apps/windows-agent/app`, regenerating `~/Veil Shared/VeilAutoInstall.iso`, powering down, and relaunching QEMU refreshed the attached media. The next `qemu-install-agent --json --wait-seconds 60` reached `agent.health.response` in 10 seconds, `app-window-proof --json --app-id winapp_notepad --wait-seconds 20` returned Korean Notepad `hwnd:00010432` plus a 600 x 393 PNG frame, `mvp-proof --json --app-id winapp_notepad --wait-seconds 20 --require-proved` returned `status=proved`, and `app-runtime-action --json --action launch --app-id winapp_notepad` moved the runtime to one mirrored Windows app window with the launcher hidden and the menu-bar action set to bring Notepad forward.

This means Veil can now distinguish "QEMU is missing" from "QEMU and the ISO are present" from "Windows Setup is actively installing on the local blank NVMe VM disk" from "Windows has reached OOBE" from "Windows has reached the desktop" from "the guest agent is reachable" from "a specific Windows app HWND is mirrored and accepts basic input." The next QEMU milestone is to make this proven CLI path the default app path: boot the installed VM, reconnect or repair the guest agent, open the mirrored app window, and hide the Windows desktop unless recovery is needed.

Live agent-install retry evidence on July 2, 2026 found one important runtime trap: regenerating `VeilAutoInstall.iso` while QEMU is already running does not update the media visible inside that VM. The old QEMU process still held the previous 900 KB ISO inode open, while the regenerated 349 MB ISO with `VeilAgent.exe` and `Bootstrap-VeilAgentFromMedia.ps1` had a new inode. A relaunch was required before Windows could see the new media. After relaunch, `lsof` verified the new QEMU PID had the current ISO inode attached. A manual PowerShell `& E:/.../Bootstrap-VeilAgentFromMedia.ps1` attempt proved the script existed but hit Windows PowerShell execution policy, so all supported bootstrap paths must keep `-ExecutionPolicy Bypass`. The accepted connection proof is now stronger than guest-agent health alone: the release gate should keep requiring a successful `veil-vmctl mvp-proof --json --app-id winapp_notepad --require-proved` response plus guest-side bootstrap/install/start logs.

References:

- Apple: [Running Linux in a Virtual Machine](https://developer.apple.com/documentation/virtualization/running-linux-in-a-virtual-machine)
- Apple: [Running macOS in a virtual machine on Apple silicon](https://developer.apple.com/documentation/virtualization/running-macos-in-a-virtual-machine-on-apple-silicon)
- Microsoft: [DiskConfiguration](https://learn.microsoft.com/en-us/windows-hardware/customize/desktop/unattend/microsoft-windows-setup-diskconfiguration)
- Microsoft: [CreatePartition](https://learn.microsoft.com/en-us/windows-hardware/customize/desktop/unattend/microsoft-windows-setup-diskconfiguration-disk-createpartitions-createpartition)
- Microsoft: [UEFI/GPT-based hard drive partitions](https://learn.microsoft.com/en-us/windows-hardware/manufacture/desktop/configure-uefigpt-based-hard-drive-partitions?view=windows-11)
- Fedora Docs: [Windows VirtIO Drivers](https://docs.fedoraproject.org/en-US/quick-docs/creating-windows-virtual-machines-using-virtio-drivers/)

## Later Boot Flow

```text
Create or load VM profile
↓
Validate Windows installer, virtual disk, shared folder, and automatic install media
↓
Run profile preflight checks
↓
Create local runtime provider configuration
↓
Boot Windows 11 Arm
↓
Guide user through Windows setup when needed
↓
Mark Windows installed after the first desktop proof
↓
Install Veil guest agent inside Windows
↓
Reconnect host to agent through QEMU `hostfwd` at ws://127.0.0.1:18444
↓
Enable app launcher and coherence windows
```

After that first setup, the normal boot loop starts from the installed system disk and skips Windows installer ISO validation/attachment. The transition is automatic once the guest agent connects; before that, the host shell's Mark Windows Installed action or `veil-vmctl mark-installed` can record the same state after the desktop is visible.

## macOS Integration Requirements

- Shared folder starts narrow and user-visible.
- Clipboard sync is opt-in until loop prevention and data-type rules are tested.
- Dock and window integration should map Windows apps to macOS affordances without showing the Windows desktop as the primary interface.
- Guest paths must never be treated as trusted host paths.

## Non-Goals

- Bundling Windows media.
- Creating a licensed Windows installation for the user.
- Claiming Microsoft or Apple endorsement.
- Fully validating the contents or bootability of a Windows installer image before the VM boot spike proves the exact requirements.
- Copying Windows media, virtual disk contents, or product keys into diagnostics bundles.
